<%= form_for @location do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class="field">
		<%= f.text_field :title %>
		<%= f.label "Nazwa miejsca (gdzie jesteś)" %>
	</div>
	<div class="field">
		<%= f.text_area :description %>
		<%= f.label "Opis (co robisz)" %>
	</div>
	<div class="field hidden">
		<%= f.text_field :latitude, :readonly => true, :id => :new_location_latitude  %>
		<%= f.text_field :longitude, :readonly => true, :id => :new_location_longitude %>
	</div>
	<div class="actions">
		<%= f.submit "Dodaj lokacje" %>
	</div>
<% end %>
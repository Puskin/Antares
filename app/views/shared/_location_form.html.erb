<%= form_for @location do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class="field">
		<%= f.label :title %>
		<%= f.text_field :title %>
	</div>
	<div class="field">
		<%= f.label :description %>
		<%= f.text_area :description %>
	</div>
	<div class="field">
		<%= f.label :latitude %>
		<%= f.text_field :latitude, :readonly => true  %>
	</div>
	<div class="field">
		<%= f.label :longitude %>
		<%= f.text_field :longitude, :readonly => true %>
	</div>
	<div class="actions">
		<%= f.submit "Dodaj lokacje" %>
	</div>
<% end %>
<div id="main" class="container">
<h1>Aktualizuj</h1>

<%= form_for(@user) do |f| %>
  
	<%= render 'fields', :f => f %>

  <div class="actions">
    <%= f.submit "Aktualizuj" %>
  </div>
<% end %>
</div>